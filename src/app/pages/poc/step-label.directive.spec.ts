import { StepLabelDirective } from './step-label.directive';

describe('StepLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new StepLabelDirective();
    expect(directive).toBeTruthy();
  });
});
