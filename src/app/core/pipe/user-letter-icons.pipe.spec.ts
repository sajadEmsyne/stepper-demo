import { UserLetterIconsPipe } from './user-letter-icons.pipe';

describe('UserLetterIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new UserLetterIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
